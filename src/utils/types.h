#pragma once

#include <stdint.h>
#include <climits>
#include <chrono>

namespace alxutils {

typedef std::chrono::system_clock::time_point TimePoint;
typedef std::chrono::milliseconds Milliseconds;


}
